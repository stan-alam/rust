## GPU programming RUST-CUDA -Linux/NVIDIA GPU

```bash
git config --global user.name "hamster"
git config --global user.email "hamster@stanthrax.com"
ssh-keygen -t ed25519 -C "hamster@stanthrax.com"
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519
cat ~/.ssh/id_ed25519.pub
ssh -T git@github.com
git clone git@github.com:stan-alam/rust.git
```